{
  "name": "hue-control-panel",
  "version": "0.0.1",
  "description": "web browser only hue light control - designed for onscreen embedded raspberry pi usage",
  "main": "bin/hue.js",
  "scripts": {
    "build": "tsc && copyfiles -f ./src/index.html bin && copyfiles resource/* bin",
    "prestart": "npm run build",
    "start": "echo done",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/erichiller/hue-control-panel.git"
  },
  "author": "Eric D Hiller",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/erichiller/hue-control-panel/issues"
  },
  "homepage": "https://github.com/erichiller/hue-control-panel#readme",
  "devDependencies": {
    "copyfiles": "^1.2.0",
    "typescript": "^2.3.2"
  }
}
